/*
#include <iostream>
#include <cmath>

double getDistance(int x1, int y1, int x2, int y2)
{
    double distance;
    distance = sqrt(pow((x2 - x1), 2) + pow((y2 - y1), 2));
    return distance;
}
int main()
{
    int x1, x2, y1, y2;
    std::cout << "Please enter the first point x1, then y1" << std::endl;
    std::cin >> x1;
    std::cin >> y1;
    std::cout << "Now enter the second point" << std::endl;
    std::cin >> x2;
    std::cin >> y2;
    std::cout << "The distance between the two points is: " <<
    getDistance(x1, y1, x2, y2)<<std::endl;
    std::cin.get();
}
 
 */
